import { DBModule } from '@dev/database';
import { Module } from '@nestjs/common';

@Module({
	imports: [
		// DBModule.forRoot({
		// 	entities: [],
		// }),
	],
	providers: [],
	controllers: [],
})
export class DomainModoule {}
